function Homepage() {
  return (
    <div className="h-screen flex flex-col justify-center items-center">
      <span>{'Go to => / user / {userID}'}</span>
      <span>userID Range 1-10</span>
    </div>
  )
}

export default Homepage
